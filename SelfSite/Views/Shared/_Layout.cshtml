<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Yauheni Liohenki - @ViewBag.Title</title>
    @Scripts.Render("~/Scripts/goad.js")
    @Styles.Render("~/Content/css")
    @Styles.Render("~/Content/Core.less?q=" + DateTime.Now.Ticks)
    @Scripts.Render("~/bundles/libs")
    @Scripts.Render("~/Scripts/cbpAnimatedHeader.js")
    @RenderSection("head")
</head>
<body id="page-top" ng-app="selfsite">
    <div class="navbar navbar-inverse navbar-fixed-top" role="navigation">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a href="@Url.Action("Index", "Home")#page-top" class="navbar-brand">YAUHENI LIOHENKI</a>
        </div>
        <div class="navbar-collapse collapse">
            @Html.MvcSiteMap().Menu()
        </div>
    </div>
    </div>
    @RenderBody()
    <footer>
        <p>&copy; @DateTime.Now.Year - Yauheni Liohenki Site</p>
    </footer>

    @RenderSection("scripts", required: false)
</body>
</html>
